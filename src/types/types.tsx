export type TInput = {
    compound?: string,
    inputType: string,
    title?: string,
    placeholderText: string,
    isActive: boolean,
    errorText?: string,
    isError?: boolean
}

export type TObject = {
    book: any,
}
